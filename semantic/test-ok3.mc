//OPIS: više deklaracija sa više promenljivih, logicki operatori, while operacije
void main() {
    int a,b;
    int c,d,e,f;
    unsigned g;
    
    if ( a > b and c < d or e >= f){
    	b = b + a;
    }
    
    while ( a < 0 and f == 1){
    	c = a + b;
    	while ( a < 0 and f == 1){
    		c = a + b;
    	}
    }
}
