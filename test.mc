int abs(int i) {
  int res;
  int a,b,c;
  if(i < 0)
    res = 0 - i;
  else 
    res = i;
  a = b + c++ - 5;
  a++;
  return res;
}

int main() {
	select a,b,c from details where (a>4);
	select a from details where (a>4);
  return abs(-5);
}
